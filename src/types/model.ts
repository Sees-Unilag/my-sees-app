export interface IYear  {
    name: string;
    value: string 
}

export interface Resource {
    name: string
    id: string
    link: string
    courseId: string
    verified: boolean
    createdAt: boolean
}